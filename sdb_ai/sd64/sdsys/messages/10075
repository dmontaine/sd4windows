Warning: the Windows profile directory for %1 was left behind.\n\nIts registry entry was removed, but the directory itself could not be. Windows\nstill has the profile's registry hive mounted, which locks a file inside it,\nand nothing can delete OR rename the directory until that is released. ONLY A\nRESTART RELEASES IT reliably.\n\nUntil the directory is gone, an account created with this name later will NOT\nget it back: Windows will not put a new profile where one already sits, and\ngives it a suffixed home instead. Anything kept in the old one, ssh keys\nincluded, will not be found there.\n\nRestart this computer, then delete the directory from an elevated prompt.
