# sd-scripts
sd install and delete scripts (AI Modified)

This is an experimental repository containing source code modified by Claud AI.  It is being tested.
For the curious or developers only.  Unlike the production system this contains the scripts and the 
sd database files in a single repository.

Warning: It can not be run in parallel with a production sd system.
